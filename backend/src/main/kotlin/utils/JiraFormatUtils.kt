// package utils

// import Story

// fun exportStoriesToJiraCsv(stories: List<Story>): String {
//     return stories.joinToString(separator = "\n", prefix = "Title,Description,Estimate\n") {
//         "${it.title},${it.description},${it.finalEstimate ?: ""}"
//     }
// }
